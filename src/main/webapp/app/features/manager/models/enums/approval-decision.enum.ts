/** `ApprovalDecisionRequest.ApprovalDecision` — the action an approver takes. */
export enum ApprovalDecision {
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
  REQUEST_MORE_INFO = 'REQUEST_MORE_INFO',
  DELEGATE = 'DELEGATE',
}
